# Frontend (Vue 3 + Vite)

该目录是医院管理系统前端项目。

## 运行命令

```bash
npm install
npm run dev
```

## 构建命令

```bash
npm run build
```

## 相关说明
- 默认后端接口前缀为 `http://localhost:8080/api`（当前在各页面内定义）。
- 项目完整说明请查看仓库根目录的 `README.md`。
